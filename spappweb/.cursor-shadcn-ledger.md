# Shadcn polish ledger (loop state)

## Done
- Full IA hubs + semantic shell + Geist + login atmospheres
- PageHeader on all admin hubs + cliente detalle
- Pipeline ClientStepper Card + primary tokens
- Empty: clientes, solicitudes, visitadores, garaje, vendidas, historial, inventario productos
- Pipeline panels: default Card, unified CardTitle
- Spot-fix: border-black → primary (visita/solicitudes/contrato/hojadevida), image-file-field tokens
- Components: scroll-area, tooltip, dropdown-menu, field, spinner, alert, empty

## Status
**Demo-ready.** Maintenance idle — no hardcode regressions found (2026-07-13 tick).

## Next ticks
- Heartbeat 45m: only clear regressions; prefer idle / stop if user asks
